{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id}}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ user.mail}}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.Roles }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': utser.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
